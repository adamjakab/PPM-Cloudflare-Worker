jobs:
  test_with_jest:
    executor:
      name: node/default
      tag: '15.0'
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm run test
version: 2.1
orbs:
  node: circleci/node@4.1.0
workflows:
  test_ppm_worker:
    jobs:
      - test_with_jest
